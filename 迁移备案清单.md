# Vercel 站迁国内 + ICP 备案 实操清单

> 目标：让 **百度等国内搜索引擎能正常添加、抓取、收录** 本站。
> 现状：纯静态站（`index.html` + `assets/` 约 4MB + `api/lead.js` 表单接口），托管在 Vercel（美国 sfo1），域名 `pinke.business`。
> 根因回顾：百度国内机房访问 Vercel 不稳定 + 域名未在大陆备案 → "添加失败"。

---

## ⚠️ 第 0 步：先核实域名能不能备案（最重要，先做这个）

ICP 备案对**域名后缀**有白名单要求，工信部只放行已通过资质审核的后缀。

- **`.business` 极大概率不在白名单内**，新顶级域名（gTLD）大多无法备案。
- **怎么确认**：在阿里云/腾讯云备案系统里填 `pinke.business`，系统会立即提示"该后缀不支持备案"。或直接咨询云服务商客服。

### 结论分两种情况

| 情况 | 处理 |
|---|---|
| `.business` **不能**备案（最可能） | 注册一个可备案后缀的新域名，推荐 **`.com`**（也可 `.cn` / `.com.cn`），如 `pinke.com` / `pinkexiaozhen.com`。后续用新域名备案，`pinke.business` 做 301 跳转到新域名 |
| `.business` 能备案（少见） | 直接用它走下面流程 |

> 备案必须用**个人或企业实名 + 域名实名认证**，域名注册者信息要和备案主体一致。企业站建议用**营业执照主体**备案。

---

## 路线总览

```
核实/更换域名 → 买大陆云服务器(备案载体) → 提交 ICP 备案(2~3周) →
备案通过后部署静态站 → 域名解析到国内服务器 → 配 HTTPS →
百度站长平台添加站点 + 验证 → 提交 sitemap → 主动推送
```

整体周期：**主要卡在备案，约 2~3 周**（管局审核）。技术部署半天能搞定。

---

## 第 1 步：购买大陆云服务器（备案的硬性载体）

备案要求服务器在**中国大陆**且是**有备案资质的服务商**。Vercel 是境外，**不能**用于备案。

- 推荐：**阿里云轻量应用服务器** 或 **腾讯云轻量应用服务器**（最便宜的 2核2G 即可，静态站绰绰有余）。
- ⚠️ 备案要求服务器购买时长 **≥ 3 个月**（部分地区要 ≥1 年），买短了无法备案。
- 买完在控制台领取/绑定一个**备案服务号**（阿里云叫"备案服务码"），备案时要用。

> 也可用 **OSS + CDN** 托管静态站，但备案同样需要先有一台 ECS/轻量服务器或购买备案码。轻量服务器最省事。

---

## 第 2 步：提交 ICP 备案

在所购云服务商的**备案控制台**操作（阿里云"备案"/腾讯云"网站备案"）：

1. 填写主体信息（个人或企业，企业需营业执照）。
2. 填写网站信息（域名、服务器、网站名称、内容类型选"企业官网/园区招商"）。
3. 上传材料：身份证/营业执照、法人/负责人照片（用 App 现场拍核验）、域名证书。
4. 部分省份要求**幕布拍照**或视频核验（云服务商会寄幕布或用 App 拍）。
5. 阿里云/腾讯云初审（1~2 天）→ 提交**管局审核**（约 1~20 个工作日，多数 7~15 天）。

> 备案期间网站可以先不上线，或先放在服务器上但**不对外解析**。备案通过会下发**ICP 备案号**，需在网站**页脚**展示并链接到 `https://beian.miit.gov.cn`（这条要加到 `index.html`，见第 5 步）。

---

## 第 3 步：部署静态站到国内服务器

本站是纯静态，部署极简单。两种方式任选：

### 方式 A：轻量服务器 + Nginx（推荐，最可控）

```bash
# 1. 服务器装 nginx
sudo apt update && sudo apt install -y nginx

# 2. 本地把站点文件上传到服务器（在项目根目录执行）
#    注意：api/ 不要传，serverless 函数在 Nginx 跑不了，见第 4 步
rsync -avz --exclude '.git' --exclude '.vercel' --exclude 'node_modules' \
  --exclude 'api' --exclude '.env*' \
  ./ root@<服务器IP>:/var/www/pinke/

# 3. 配置 nginx 站点（/etc/nginx/sites-available/pinke）
```

Nginx 配置示例：

```nginx
server {
    listen 80;
    server_name pinke.com www.pinke.com;   # 换成你的备案域名
    root /var/www/pinke;
    index index.html;
    location / { try_files $uri $uri/ /index.html; }
    # 静态资源缓存
    location ~* \.(jpg|jpeg|png|webp|css|js|svg)$ {
        expires 30d;
        add_header Cache-Control "public";
    }
}
```

```bash
sudo ln -s /etc/nginx/sites-available/pinke /etc/nginx/sites-enabled/
sudo nginx -t && sudo systemctl reload nginx
```

### 方式 B：阿里云 OSS + CDN（适合纯静态、不想管服务器）

- 建 OSS Bucket → 开"静态网站托管" → 上传所有文件（除 `api/`）。
- 绑定备案域名 + 开 CDN 加速 + 配 HTTPS 证书。
- 表单接口用"函数计算 FC"或云服务器单独承载。

---

## 第 4 步：处理 `api/lead.js`（表单提交接口）

Vercel 的 serverless 函数迁国内后**不能直接用**，需要替代方案（按省事排序）：

1. **最省事**：表单改用第三方表单服务（如腾讯问卷/金数据/麦客 CRM），`index.html` 里把提交目标换成它们的接口，不用自己写后端。
2. **保留自有后端**：在国内服务器上用 Node 跑一个小 Express 服务承接 `/api/lead`，Nginx 反代到该端口。把 `api/lead.js` 的逻辑搬过去即可。
3. **用云函数**：阿里云函数计算 FC / 腾讯云 SCF 部署 `lead.js`，绑定 API 网关，前端改请求地址。

> 先确认 `api/lead.js` 当前把数据发到哪里（邮箱？飞书？数据库？），再决定。迁移时这是唯一需要改代码的地方。

---

## 第 5 步：HTTPS + 备案号页脚

- **HTTPS**：轻量服务器用 `certbot` 免费签 Let's Encrypt，或在云服务商申请免费 DV 证书绑定。
  ```bash
  sudo apt install -y certbot python3-certbot-nginx
  sudo certbot --nginx -d pinke.com -d www.pinke.com
  ```
- **页脚加备案号**（备案通过后）：在 `index.html` 页脚加一行
  ```html
  <a href="https://beian.miit.gov.cn" target="_blank" rel="nofollow">
    粤ICP备XXXXXXXX号
  </a>
  ```
  （深圳属广东，备案号前缀为"粤ICP备"。这是合规要求，不加可能被注销备案。）

---

## 第 6 步：域名解析切换

1. 在域名 DNS 处（阿里云/腾讯云 DNS 控制台）把 `@` 和 `www` 的 A 记录指向**国内服务器 IP**（或 CDN 的 CNAME）。
2. 如果换了新域名，在 Vercel 上把旧的 `pinke.business` 配置 **301 永久跳转**到新域名，保留已有的一点权重。
3. 用 `ping`/`curl` 确认国内能正常访问、HTTPS 正常。

---

## 第 7 步：回到百度站长平台添加站点

备案通过 + 站点国内可访问后：

1. **添加网站**：填 `https://你的备案域名`，这次"下一步"百度蜘蛛能抓到首页，不会再"添加失败"。
2. **验证站点**：选"文件验证"——下载百度给的验证文件传到站点根目录（Nginx 的 `/var/www/pinke/`），或用"HTML 标签验证"把 meta 加到 `index.html` 的 `<head>`。
3. **提交 sitemap**：本站已有 `sitemap.xml`，在"普通收录 → sitemap"提交 `https://你的域名/sitemap.xml`。
4. **主动推送**：本站 `index.html` 里已埋了百度主动推送代码（`push.zhanzhang.baidu.com`），确认 token 是新站的，加速收录。
5. （可选）开通"百度统计"，做流量监测。

---

## 同步要改的项目文件（换域名后）

如果更换了域名，全站需要把 `pinke.business` 替换成新域名：

- `index.html`：canonical、og:url、JSON-LD 里的 url、百度推送 token
- `sitemap.xml`：所有 `<loc>`
- `robots.txt`：Sitemap 地址
- `llms.txt`：站点 URL
- `config.js`：如有域名相关配置

> 改完我可以帮你一次性全局替换并校验。

---

## 成本预估（年）

| 项 | 费用 |
|---|---|
| 大陆轻量服务器 2核2G | ~70~120 元/年（新人活动价） |
| 新域名 `.com` | ~60~80 元/年 |
| SSL 证书 | 免费（Let's Encrypt / 云厂商免费 DV） |
| 备案 | 免费 |
| **合计** | **约 150~200 元/年** |

---

## 一句话决策

- **要吃百度国内流量** → 按上面走，绕不开"国内服务器 + 备案"，重点先核实域名后缀能否备案。
- **短期不急** → 先把国产 AI 搜索（豆包/Kimi/文心）的 GEO 做厚，备案并行推进，通过后再切百度。
